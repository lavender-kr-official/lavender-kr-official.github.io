# 라벤더 (lavender) — 건설·토목·건축 정보 큐레이션

건설 실무자가 흩어진 공고를 한곳에서 보게 하는 개인 프로젝트.
텔레그램 채널(하루 1회 아침 다이제스트) + GitHub Pages 사이트.
1인 운영, 무료, 광고 없음, 회원가입 없음.

## 실행

```bash
pip install -e ".[dev]"
python -m pytest tests/ -q            # 164건, 1.5초

python -m src.main collect --dry-run  # 수집만, 게시·상태변경 없음
python -m src.main probe --target <URL 또는 소스id>   # 소스 진단 (콤마로 여러 개)
python -m src.main digest-preview --days 1           # 다이제스트 모양 확인
python -m src.main requeue --intent recruit --dry-run # 묻힌 항목 되살리기
python -m src.main verify-sources
```

시크릿은 **GitHub Actions Secrets에만** 둔다. 코드·저장소에 절대 넣지 않는다.
로컬에서 게시까지 시험하려면 환경변수로 주입하되 파일로 남기지 않는다.

## 구조

`config/sources.yaml`이 수집 소스의 유일한 선언처다. 코드 수정 없이 소스를 늘린다.
수집기 type: `rss` / `board`(범용 HTML 게시판) / `eminwon`(새올 표준, host만 교체) /
`narajangteo`(나라장터 API) / `youtube_rss` / `naver_news`.

상태는 `state/seen.db` (SQLite, 저장소에 커밋됨). 테이블은 `seen`, `meta` 둘뿐.
상태 전이: seen → pending → posted / skipped.

파이프라인: 수집 → 키워드 필터 → 의도 분류(`src/intent.py`) → 중복 제거 →
우선순위 정렬 → 상한 → 다이제스트 1통 → 사이트 빌드.

## 지켜야 할 원칙

1. **본문을 수집·저장하지 않는다.** 제목·링크·기관·날짜만. 목록 페이지를 하루 1회
   읽고 상세 페이지는 열지 않는다. 법적 안전성의 근간이고, 법률 검토가 이 전제 위에서
   이뤄졌다. 깨야 한다면 사용자에게 먼저 알린다.
2. **robots.txt를 지킨다.** `src/robots.py`. board·eminwon 모두 게이트를 통과한다.
   금지된 호스트는 조용히 건너뛰지 않고 CollectError로 올린다 — 0건과 거부는 다르다.
3. **명단·위촉 결과 공고는 게시하지 않는다.** 지원할 수 없는 정보이고 사람 이름이 섞인다.
   DB에는 남긴다(기수·임기 단서).
4. **차단 우회를 만들지 않는다.** 봇 차단 페이지가 오면 정식 경로를 찾거나 포기한다.
5. **진단 커맨드는 워크플로를 실패시키지 않는다.** 설정 미완료는 고장이 아니다.
   늑대 소년이 되면 진짜 고장을 놓친다. (예외: smoke-g2b — 막히면 입찰이 통째로 죽는다)

## 실측으로 확정된 사실 (추측 아님, 재조사 불필요)

**접근 차단 — GitHub 러너(해외 IP)에서**
- `ksce.or.kr`(대한토목학회): httpx 45초 무응답, curl exit 28. 전송 계층에서 끊김
- `kpea.or.kr`(한국기술사회): httpx "Server disconnected", curl exit 52 "Empty reply"
- 위 둘은 **해외 IP 차단 확진.** 국내 IP 경유(릴레이/셀프호스티드 러너) 없이는 불가
- `calspia.go.kr`: 페이지는 45KB로 오지만 결과 테이블 `#list`,`#list2`가 tr=0 — AJAX.
  서버렌더 HTML에 데이터가 없어 현 수집기로 불가능
- 개발 컨테이너의 프록시는 **모든 한국 도메인**을 CONNECT 403으로 막는다.
  따라서 사이트 확인은 Actions의 `probe` 커맨드로만 가능하고, 결과는
  `state/probe-latest.txt`에 커밋되어 돌아온다

**협회 경유 링크**
- 대한건축사협회 상세 페이지에는 **본문이 없다.** 제목·작성자(항상 협회)·첨부 hwp뿐
- 대한건축학회 상세 페이지에는 본문이 있으나 **원 기관 URL이 없다**
- → "링크를 원 기관으로 돌리기"는 데이터가 없어 불가능. 대신 목록에서 기관명을 얻는다:
  건축학회는 글쓴이 칸, 건축사협회는 제목 앞 두 번째 대괄호(`title_tags` 옵션)

**위원회 임기 (법령 조문 확인)**
- 도시계획위·지방건설기술심의위·기술자문위·경관위 = **임기 2년, 시행령 법정**
- 건축위원회만 조례 위임(3년 이내 상한) → 지자체마다 다름
- 설계공모 심사위원·제안서평가위원 = 임기 개념 없음(건별 구성 + 상시 풀 등록)
- **법령은 임기의 '길이'만 정하고 '기산일'은 정하지 않는다.** 기관별 직전 위촉일을
  알아야만 다음 모집 시점이 계산된다. 그 값은 공고문에 있지 제목에는 없다
- 연임이 허용되므로 임기 만료가 공모를 보장하지 않는다 (예측 적중률 13~30% 추정)

**데이터 분포 (2026-09, 9일치 1,970건)**
- 뉴스 1,054 / 입찰 830 / 정책 36 / 위원회 24
- 위원회 24건 중 **실제 지원 가능한 위원 모집은 6건**. 나머지는 설계공모·세미나·명단
- 입찰 830건 중 추정가 10억 이상은 80건(9%) → 다이제스트는 10억 이상만 싣는다
- 기초지자체 새올 게시판 1곳(기장군) = 하루 30건, 그중 위원 모집 0건.
  **226곳을 붙이면 하루 6,800건을 긁어 몇 건 건지는 구조가 된다 — 권하지 않음**

## 과거에 실제로 터졌던 버그 (회귀 주의)

- **부트스트랩 손실**: 첫 수집 16건 중 3건만 게시되고 13건이 `seen`으로 영영 묻혔다.
  진짜 위원 모집 6건이 전부 여기 있었고, 대신 세미나·방송 출연 모집이 나갔다.
  → `src/intent.py` 우선순위 정렬로 해결. 묻힌 것은 `requeue`로 꺼낸다
- **pending 재전송**: `_item_from_row`가 key_prefix/natural_key를 복원하지 않아
  dedup_key가 URL 해시가 됐고 `mark_posted`가 0행을 갱신했다. 매일 재전송되는 버그
- **SQLite 시각 비교**: ISO 문자열을 `datetime('now','-N minutes')`와 비교하면
  `'T' > ' '`라 전부 매칭된다. 컷오프는 파이썬에서 ISO로 계산해 넘긴다
- **LH 게시판**: `href`가 전부 `#none`이고 실제 주소는 `onclick` 안에 있다
- **매체 상한이 분야 상한에 무너짐**: 라운드로빈 없이 상한만 걸면 발행량 많은 매체가
  앞을 다 채운다 (`_balance_by_source`)

## 작업 규칙

- 브랜치 `claude/construction-sns-platform-1icl7s`에만 커밋·푸시
- PR은 명시적으로 요청받았을 때만
- 커밋 메시지는 한국어. **무엇을 왜 고쳤는지, 근거가 된 실측을 함께 적는다**
- 새 소스를 켜기 전 반드시 `probe`로 실제 파싱 결과를 확인한다.
  "몇 행이 셀렉터에 걸렸나"가 아니라 "Item이 몇 건 나오나"가 판정 기준이다
